# 实施计划

## 1. 修改 SOL 策略

- [x] 增加四路、退出负责人和 R1 契约头。
- [x] 增加 5% 固定止损及 `tradeDirection long`。
- [x] 添加 `df.copy()`。
- [x] 将金叉/死叉转换为四路边缘信号。
- [x] 更新图表标记数据源。

## 2. 修改 BTC 策略

- [x] 保留 `entryPct 0.9`。
- [x] 增加四路、退出负责人和 R2 契约头。
- [x] 将固定止损从 30% 改为 5%。
- [x] 修复 pandas 3.x 边缘触发。
- [x] 将超卖/超买转换为四路反手信号。
- [x] 更新图表标记数据源。

## 3. 验证

- [x] 对两份文件执行 Python 编译检查。
- [x] 使用后端虚拟环境运行 `validate_indicator_code`。
- [x] 用构造数据验证信号列、长度、dtype 和边缘去重。
- [x] 用构造回测验证 R2 同一根 K 线先平仓、再反向开仓。
- [x] 搜索未来函数及非法策略键。
- [x] 运行 `git diff --check`。
- [x] 审查最终 diff，确认未修改 P2/P3 和其他业务文件。

## 4. 风险与检查点

- PowerTower 的既有 `entryPct 0.9` 是用户修改，必须保留。
- 四路迁移会改变策略代码契约，但保持原有多空/反手交易意图。
- 止损从 30% 改为 5% 会改变历史回测结果，因此本任务不沿用文件名中的旧收益作为新结果。
- 本任务不自动同步或重启已运行实例。
